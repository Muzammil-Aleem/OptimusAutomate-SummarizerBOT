streamlit>=1.38.0
pypdf>=4.3.0
anthropic>=0.40.0
scikit-learn>=1.4.0
numpy>=1.26.0

# Optional upgrade: real sentence embeddings instead of TF-IDF retrieval.
# Heavier install (pulls in torch), but more semantically accurate.
# Uncomment to enable — the app auto-detects and uses it if installed:
sentence-transformers>=3.0.0
